<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>환경 프로젝트</title>
    <style>
        /* 전역 스타일 */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Arial', sans-serif;
}

span {
    display: flex;
    border: none;
    width: 100%;
    height: 100%;
}

p {
    display: table-cell;
}

a {
    text-decoration: none;
    color: #333;
    display: block;
}

li {
    list-style-type: none;
}

.out_main {
    width: 100%;
    min-height: 100vh;
    background-color: #f4f4f4;
}

/* 헤더 */
.header {
    width: 100%;
    height: 80px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 10%;
    position: fixed;
    top: 0;
    z-index: 1000;
    background-color: #009688;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.logo h1 {
    color: white;
    font-weight: bold;
    font-size: 24px;
}

.menu {
    font-size: 18px;
    font-weight: bold;
    display: flex;
}

.menu li {
    padding: 15px 20px;
}

.menu li a {
    color: white;
    transition: background-color 0.3s ease;
}

.menu li a:hover {
    background-color: #00796b;
    border-radius: 4px;
}

/* 본문 */
.main {
    width: 100%;
    padding-top: 100px; /* 헤더 크기만큼 패딩 추가 */
}

.present {
    width: 80%;
    margin: 0 auto;
    background-color: white;
    padding: 30px;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

/* 제목 입력 필드 스타일 */
.title-input {
    width: 100%;
    padding: 10px;
    margin: 10px 0;
    border: 1px solid #ccc;
    border-radius: 4px;
}

/* 본문 텍스트 박스 스타일 */
.content-textarea {
    width: 100%;
    height: 150px;
    padding: 10px;
    margin: 10px 0;
    border: 1px solid #ccc;
    border-radius: 4px;
    resize: vertical; /* 수직으로 크기 조절 가능 */
    display:table;
    table-layout: fixed;
}

/* 푸터 */
.footer {
    width: 100%;
    height: 80px;
    background-color: #009688;
    margin-top: 40px;
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    font-weight: bold;
    font-size: 16px;
}


.upload_ {
    display:flex;
}
/* 업로드 박스 스타일 */
.upload {
    width: 150px;
    height: 150px;
    margin: 10px;
    text-align: center;
    border: 2px dashed #009688; /* 대시선 스타일 */
    border-radius: 8px; /* 둥근 모서리 */
    background-color: #f9f9f9; /* 배경색 */
    padding-top: 15px;
    transition: background-color 0.3s ease, border-color 0.3s ease; /* 부드러운 전환 효과 */
}
.upload img{
    width: 120px;
    height: 120px;
    border-radius: 8px;
    transition: background-color 0.3s ease, border-color 0.3s ease;
}

.upload:hover , .upload img:hover{
    background-color: #e0f2f1; /* 마우스 오버 시 배경색 변경 */
    border-color: #00796b; /* 마우스 오버 시 테두리 색 변경 */
}

.upload_button {
    padding: 12px 24px;
    text-align: center;
    background-color: #009688; /* 기본 배경색 */
    color: white; /* 글자색 */
    border: none; /* 기본 테두리 제거 */
    border-radius: 4px; /* 둥근 모서리 */
    font-size: 16px; /* 글자 크기 */
    font-weight: bold; /* 글자 두껍게 */
    cursor: pointer; /* 마우스 커서 포인터로 변경 */
    transition: background-color 0.3s ease, transform 0.2s; /* 배경색 및 변환 효과 */
    outline: none; /* 포커스 아웃라인 제거 */
}

.upload_button:hover {
    background-color: #00796b; /* 마우스 오버 시 배경색 변경 */
    transform: translateY(-2px); /* 버튼을 약간 위로 이동 */
}

.upload_button:active {
    background-color: #004d40; /* 클릭 시 배경색 변경 */
    transform: translateY(0); /* 클릭 시 원래 위치로 복귀 */
}


.image-container {
    display: block;
    width: 100%; /* 부모 요소의 전체 너비를 차지 */
    margin: 20px 0; /* 위아래 여백 추가 */
}

.image-container img {
    max-width: 100%; /* 이미지가 부모 요소의 너비를 넘지 않도록 설정 */
    height: auto; /* 이미지 비율 유지 */
}

.text-container {
    width: 100%;
    margin: 10px 0; /* 텍스트와 이미지 사이 여백 */
}

    </style>
</head>

<body>
    <div class="out_main">
        <header class="header">
            <div class="logo">
                <h1>환경 프로젝트</h1>
            </div>

            <!-- 메뉴 -->
            <nav>
                <ul class="menu">
                    <li><a href="/main">홈페이지</a></li>
                    <li><a href="/listpage">게시판</a></li>
                    <li><a href="/loginpage" target="_parent">로그인</a></li>
                    <li><a href="/signpage" target="_parent">회원가입</a></li>
                </ul>
            </nav>
        </header>

        <!-- 본문 부분 -->
        <main class="main">
            <div class="present">
            <form action="/write" id="form" method="post">
                <h2>제목 입력</h2>
                <input type="text" placeholder="제목을 입력하세요." class="title-input" id="title-input" name="title">

                <h2>본문</h2>
                <div class="content-textarea" id="content-textarea">
                    <span laceholder="본문 내용을 입력하세요." contenteditable="true" id="contentEditor"></span>
                </div>

                <div class="upload_">
                    <textarea name="textarea" id="myTextarea" cols="30" rows="10"></textarea>
                    <label for="upload">업로드(최대 5개)</label>
                    <input type="file" id="upload" hidden/>
                    <!--
                    <div class="upload" id="imageUpload">
                        <label class="label" id="label" for="input">
                            <div class="inner" id="inner">이미지 업로드</div>
                        </label>
                        <input id="imageInput" class="input" accept="image/*" type="file" multiple="true">
                    </div>
                    -->
                </div>
                <div id="secret room">
                <input id="content-input" name="content" style="display:none">
                <input id="link1" name="link1" style="display:none" type="text">
                <input id="link2" name="link2" style="display:none" type="text">
                <input id="link3" name="link3" style="display:none" type="text">
                <input id="link4" name="link4" style="display:none" type="text">
                <input id="link5" name="link5" style="display:none" type="text">
                </div>
            </form>
                <div><button class="upload_button" onclick="submitForm()">게시</button></div>
            </div>
        </main>

        <!-- 푸터 부분 -->
        <footer class="footer">
            <!-- 푸터 내용이 없습니다. -->
        </footer>
    </div>

    <script>
        let num=0;
        const lin1 = document.getElementById("link1");
        const lin2 = document.getElementById("link2");
        const lin3 = document.getElementById("link3");
        const lin4 = document.getElementById("link4");
        const lin5 = document.getElementById("link5");
        
        function uploadFile() {
        const textarea = document.getElementById("myTextarea");
        const fileInput = document.getElementById("upload");
    
        const upload = (file) => {
            if (file && file.size < 5e6) {
                const formData = new FormData();
    
                formData.append("image", file);
                fetch("https://api.imgur.com/3/image", {
                    method: "POST",
                    headers: {
                        Authorization: "Client-ID 9fd239bc7576d54",
                        Accept: "application/json",
                    },
                    body: formData,
                })
                    .then((response) => response.json())
                    .then((response) => {
                        if (response.success) {
                            const { data } = response;
                            textarea.value += `${textarea.value ? "\n" : ""}${
                                data.animated && data.mp4 ? data.mp4 : data.link
                            }`;
                            console.log('링크: '+response.data.link);
                            if(num==0)
                                lin1.value=response.data.link;
                            else if(num==1)
                                lin2.value=response.data.link;
                            else if(num==2)
                                lin3.value=response.data.link;
                            else if(num==3)
                                lin4.value=response.data.link;
                            else if(num==4)
                                lin5.value=response.data.link;
                            console.log("lin12345 : "+lin1.value+lin2.value+lin3.value+lin4.value+lin5.value);
                            num++;
                        } else {
                            console.error("업로드 실패");
                        }
                    })
                    .catch((error) => {
                        console.error(error);
                    });
            } else {
                console.error("파일 용량 초과");
            }
        };
    
        const handleDrop = (event) => {
            event.preventDefault();
    
            const { files } = event.dataTransfer;
    
            if (files.length) {
                upload(files[0]);
            }
        };
    
        const handlePaste = (event) => {
            event.preventDefault();
    
            const { files } = event.clipboardData;
    
            if (files.length) {
                upload(files[0]);
            }
        };
    
        const preventDefault = (event) => {
            event.preventDefault();
        };
    
        if (fileInput) {
            fileInput.addEventListener("change", () => {
                upload(fileInput.files[0]);
            });
        }
    
        if (textarea) {
            textarea.addEventListener("dragenter", preventDefault);
            textarea.addEventListener("dragleave", preventDefault);
            textarea.addEventListener("dragover", preventDefault);
            textarea.addEventListener("drop", handleDrop);
            textarea.addEventListener("paste", handlePaste);
        }
    }
    uploadFile();
    

        function validateForm() {
            const iddom = document.getElementById("title-input");
            const contentdom = document.getElementById("contentEditor");
            const passworddom = document.getElementById("content-input");
            passworddom.value=contentdom.innerHTML.replace("<br>", "</span><br><span>");
            const title = iddom.value.trim();
            const content = passworddom.value;
            console.log("content: "+content);
            
            if (title.length > 255) {
                alert("제목을 255자 내로 입력해주세요.");
                return false;
            } else if (title === "") {
                alert("아이디를 입력해주세요.");
                return false;
            } else if (content === "") {
                alert("비밀번호를 입력해주세요.");
                return false;
            } else {
                /*
                const thing = async (s = content) => {
                    const shrunk = await compress(s)
                    passworddom.value=shrunk;
                }
                */
                return true
            }
        }

        function submitForm() {
            if (validateForm()) {
                document.getElementById("form").submit();
            }
        }
    </script>
</body>

</html>
