<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>환경프로젝트</title>
    <style>
    /* 전역 스타일 */
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Arial', sans-serif;
    }

    a {
        text-decoration: none;
        color: #333;
        display: block;
    }

    li {
        list-style-type: none;
    }

    .out_main {
        width: 100%;
        min-height: 100vh;
        background-color: #f4f4f4;
    }

    /* 헤더 */
    .header {
        width: 100%;
        height: 80px;

        display: flex;
        justify-content: space-between;
        align-items: center;

        padding: 0 10%;
        position: fixed;
        top: 0;
        z-index: 1000;

        background-color: #009688;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .logo h1 {
        color: white;
        font-weight: bold;
        font-size: 24px;
    }

    .menu {
        font-size: 18px;
        font-weight: bold;

        display: flex;
    }

    .menu li {
        padding: 15px 20px;
    }

    .menu li a {
        color: white;
        transition: background-color 0.3s ease;
    }

    .menu li a:hover {
        background-color: #00796b;
        border-radius: 4px;
    }


    /* 본문 */
    .main {
        width: 100%;
        padding-top: 100px; /* 헤더 크기만큼 패딩 추가 */
    }

    .present {
        width: 80%;
        margin: 0 auto;
        background-color: white;
        padding: 30px;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    /* 게시판 */
    .post h4 {
        font-size: 28px;
        margin-bottom: 20px;
        color: #333;
    }

    /* 새로 추가된 스타일 */
    .post-link {
        text-decoration: none;
        color: #333;
        display: inline-block;
        transition: color 0.3s ease;
    }

    .post-link:hover {
        color: #009688;
        cursor: pointer;
    }

    /* 게시판 ul 스타일 */
    .post ul {
        border: 1px solid #ddd;  /* ul의 외곽선 */
        padding: 15px 20px;      /* ul 내부의 패딩 */
        border-radius: 4px;
        background-color: #f9f9f9;
    }

    /* 게시판 항목 스타일 */
    .post ul li {
        padding: 10px 0;       /* 각 항목의 위아래 패딩 */
        border-bottom: 1px solid #ccc; /* 항목 사이의 구분선 */
    }

    .post ul li:last-child {
        border-bottom: none; /* 마지막 항목의 구분선 제거 */
    }

    /* 푸터 */
    .footer {
        width: 100%;
        height: 80px;
        background-color: #009688;
        margin-top: 40px;
        display: flex;
        justify-content: center;
        align-items: center;
        color: white;
        font-weight: bold;
        font-size: 16px;
    }
    </style>
    <!-- <link rel="stylesheet" href="style.css"> -->
</head>

<body>
    <div class="out_main">
        <div class="header">
            <div class="logo">
                <h1>환경 프로젝트</h1>
            </div>

            <!-- 메뉴 -->
            <ul class="menu">
                <li><a href="/main">홈페이지</a></li>
                <li><a href="/listpage?page=1">게시판</a></li>
                <li><a href="/loginpage" target="_parent">로그인</a></li>
                <li><a href="/signpage" target="_parent">회원가입</a></li>
            </ul>

        </div>

        <!-- 본문 부분 -->
        <div class="main">
            <!-- present는 글이 들어가는 본문 부분 -->
            <div class="present">

                 <!-- 게시판 -->
                  <div class="post" id="post">
                    <h4><a href="post/post.html" class="post-link">게시판</a></h4>

                    <ul>
                        <% for(var i=0;i<cnt;i++) { %>
                            <li><a href="./view?id=<%= datas[i].id %>" class="row item">
                                <div class="col title ellipsis"><%= datas[i].title %></div>
                                <div class="col_writer"><%= datas[i].writer %></div>
                                <div class="col_date"><%= datas[i].regdate %></div>
                            </a></li>
                        <% } %>
                        <!-- <% for(var i=0;i<maxcnt-cnt;i++) { %>
                            <li><a class="row item"><div class="col title ellipsis"></div><div class="col_date"></div><div class="col_writter"></div></a></li>
                        <% } %> -->
                    </ul>

                  </div>
            </div>

        </div>
        <div class="footer"></div>
    </div>
</body>

</html>